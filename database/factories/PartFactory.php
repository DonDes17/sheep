<?php

use Faker\Generator as Faker;

$factory->define(Model::class, function (Faker $faker) {
    return [

    /*'day'=> $faker->numberBetween(0,31),
    'started'=> $faker->date($format = 'Y-m-d', $max = 'now'),
    'ended'=> $faker->date($format = 'Y-m-d', $max = 'now'),*/

    ];
});
